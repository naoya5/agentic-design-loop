export const QA_EVALUATOR_FEW_SHOT_EXAMPLES = `
### 例1: スコア ~4（低品質）

スプリントコンテキスト:
TodoアプリのCRUD機能を実装するスプリント。
要件: タスクの追加・編集・削除・完了チェック、ローカルストレージへの永続化。

テスト結果:
- タスク追加: 入力後Enterキーで追加できない（ボタンクリックのみ）→ FAIL
- タスク編集: ダブルクリックで編集モードに入れず、編集機能が見当たらない → FAIL
- タスク削除: 削除ボタンはあるが確認なしで即削除、誤操作リスクが高い → 動作はするがUX問題
- 完了チェック: チェックボックスをクリックしても状態が変わらない → FAIL（コンソールにエラーあり）
- ローカルストレージ: ページリロード後にデータが消える → FAIL
- コンソールエラー: "Cannot read properties of undefined (reading 'map')" が複数回発生

コード確認:
- 単一ファイル500行のスパゲッティコード
- 変数名がa, b, c等の意味不明なもの
- エラーハンドリング皆無
- コンポーネント分割なし

評価:
- productDepth: 3/10 - 仕様の主要機能の半分以上が未実装または動作不良
- functionality: 3/10 - 完了チェック・永続化という核心機能が動作しない。コンソールエラー多発
- visualDesign: 4/10 - 最低限のスタイルは当たっているが、デザインの意図が感じられない
- codeQuality: 3/10 - 単一ファイルにすべて詰め込まれており、変数名も不明瞭。保守不可能

averageScore: 3.25
criticalIssues: ["完了チェック機能が動作しない（コンソールエラーあり）", "ローカルストレージへの永続化が実装されていない", "編集機能が要件通り実装されていない"]
strengths: ["タスク追加の基本フロー（ボタン経由）は動作する", "削除ボタンは視認性が高い"]

---

### 例2: スコア ~6（中品質）

スプリントコンテキスト:
天気予報アプリの検索・表示機能スプリント。
要件: 都市名検索、現在の天気・5日間予報の表示、ローディング状態・エラー状態のハンドリング。

テスト結果:
- 都市名検索: 入力してEnterまたはボタンクリックで検索できる → PASS
- 現在の天気表示: 気温・天気状態・アイコンが正しく表示される → PASS
- 5日間予報: 表示されるが同じ日付が重複する場合がある（APIレスポンスの処理が不正確）→ 部分PASS
- ローディング状態: スピナーは表示されるがテキストの「読み込み中...」が英語のまま → 軽微
- エラー状態: 存在しない都市を入力した場合にエラーメッセージが表示される → PASS
- 空文字検索: 何も入力せずに検索するとAPIエラーが表示される（バリデーションなし）→ FAIL
- コンソールエラー: なし

コード確認:
- コンポーネントは3つに分割されている（App, WeatherCard, ForecastList）
- API呼び出しは別ファイルに分離されている
- TypeScriptの型定義があるが、一部anyが使われている
- エラーハンドリングはtry-catchで実装されているが、エラー種別の区別なし

評価:
- productDepth: 6/10 - 主要機能の大部分は実装されているが、予報表示の重複バグと入力バリデーション不足がある
- functionality: 6/10 - 主要フローは動作するが、エッジケース（空文字、API重複データ）の処理が不完全
- visualDesign: 7/10 - 天気アイコンの使い方が適切で、情報の優先順位が視覚的に明確
- codeQuality: 6/10 - コンポーネント分割は適切だが、TypeScriptのany使用とエラー種別の未区別が惜しい

averageScore: 6.25
criticalIssues: ["空文字での検索でバリデーションなしにAPIエラーが露出する", "5日間予報に重複エントリが表示される場合がある"]
strengths: ["API呼び出しがコンポーネントから分離されている", "エラー状態の表示が実装されている", "天気アイコンによる情報の視覚化が適切"]

---

### 例3: スコア ~8（高品質）

スプリントコンテキスト:
Kanbanボードアプリのドラッグ＆ドロップ機能スプリント。
要件: カード間のドラッグ＆ドロップ、列間の移動、順序の永続化、キーボードアクセシビリティ。

テスト結果:
- ドラッグ＆ドロップ（マウス）: カードを列内・列間でスムーズに移動できる → PASS
- 列間移動: ドロップ時に正しい位置に挿入される（上下の判定が正確）→ PASS
- 永続化: ページリロード後も順序が保持される（IndexedDB使用）→ PASS
- キーボード操作: Space/Enterでピック、矢印キーで移動、Enterでドロップが動作 → PASS
- ドラッグ中のビジュアル: ドラッグ中の要素がゴースト表示になり、ドロップ先インジケーターが明確 → PASS
- 並行操作: 複数のカードを素早く連続移動しても順序が乱れない → PASS
- スクリーンリーダー: aria-live regionでドラッグ状態のアナウンスあり → PASS
- コンソールエラー: なし

コード確認:
- カスタムフック（useDragAndDrop）にドラッグロジックが完全に分離されている
- 全コンポーネントにTypeScript厳密型付け（noImplicitAny）
- アクセシビリティのためのARIA属性が適切に実装されている
- テストファイルが各コンポーネントに存在し、主要インタラクションがカバーされている

評価:
- productDepth: 9/10 - 仕様のすべての要件を満たし、アクセシビリティ対応まで実装している
- functionality: 8/10 - 主要フローと多くのエッジケースが正確に動作する
- visualDesign: 7/10 - ドラッグ中のビジュアルフィードバックが明確で、インタラクションの状態が視覚的にわかりやすい
- codeQuality: 9/10 - カスタムフックへの分離、厳密な型付け、テストカバレッジが高い水準

averageScore: 8.25
criticalIssues: ["スマートフォンのタッチ操作で稀にドロップ位置の判定がずれる（タッチデバイスのPointerEvent処理の改善余地あり）"]
strengths: ["キーボードアクセシビリティが完全に実装されている", "カスタムフックへのロジック分離が徹底されており保守性が高い", "IndexedDBによる永続化で大量データでもパフォーマンスが安定", "テストファイルが存在し品質保証の基盤がある"]
`;
